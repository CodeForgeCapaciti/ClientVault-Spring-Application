apiVersion: v1
kind: Service
metadata:
  name: clientvault-service
  namespace: clientvault
spec:
  type: NodePort
  selector:
    app: clientvault
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30007
